User-agent: <%= currentNode["user-agent"] %>
<%
  var allow = currentNode["allow"];
  var len = allow.length;
  for (int i = 0; i < len; i++) {
    out.println("Allow: " + allow[i]);
  }
%>
Sitemap: <%= currentNode["sitemap"] %>
