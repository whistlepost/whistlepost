User-agent: <%= currentNode["user-agent"] %>
<%
  var allowed = currentNode["allow"];
  var len = allowed.length;
  for (int i = 0; i < len; i++) {
    out.println("Allow: " + allowed[i]);
  }
%>
Sitemap: <%= currentNode["sitemap"] %>
